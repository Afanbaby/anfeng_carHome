<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lanou3g.an.carhome">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_carhome"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".welcome.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".articleNestingFragment.bulletin.bulletinDetail.BulletinDetailActivity"/>
        <activity android:name=".main.MainActivity"/>
        <activity android:name=".articleNestingFragment.newest.newestDetail.NewestDetailAvtivity"/>
        <activity android:name=".articleNestingFragment.video.videoDetail.VideoDatailActivity"/>
    </application>

</manifest>